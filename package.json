{
	"name": "koopa",
	"version": "2.0.0",
	"keywords": ["browser", "useragent", "user-agent", "sniff", "user", "agent", "sniffing"],
	"description": "Browser sniffing for node/browser",
	"author": {
		"name": "Tommy Montgomery",
		"email": "tmont@tmont.com",
		"url": "http://tmont.com/"
	},

	"files": [
		"./src/koopa.js"
	],

	"main": "./src/koopa.js",

	"bin": "./bin/koopa-cli.js",

	"devDependencies": {
		"mocha": ">=1.3.2",
		"expect.js": ">=0.1.2",
		"serve": ">=1.1.0"
	},

	"directories": {
		"lib": "./src"
	},

	"scripts": {
		"test": "./node_modules/.bin/mocha --ui bdd --reporter dot tests",
		"start": "./node_modules/.bin/serve --port 3333 ."
	},

	"repository": {
		"type": "git",
		"url": "https://github.com/tmont/koopa.git"
	}
}
